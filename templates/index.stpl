<html>
  <head>
    <% include!("head.stpl"); %>
  </head>
  <body>
    <% include!("nav.stpl"); %>
    <main>
      <% if user.is_some() { %>
        <a href="/thread/create">
          <button>Create new thread</button>
        </a>
      <% } else { %>
        <p>You need to login or create a user in order to create threads.</p>
      <% } %>
      <% include!("threads.stpl"); %>
    </main>
  </body>
</html>
