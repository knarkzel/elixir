<html>
  <head>
    <% include!("head.stpl"); %>
  </head>
  <body>
    <% include!("nav.stpl"); %>
    <main>
      <ul>
      <% for post in posts { %>
        <li>id: <%= post.id %> with content: <pre><%= post.content %></pre></li>
      <% } %>
      </ul>
      <p>
        Let's say you have a product, a portfolio, or just an idea you want to
        share with everyone on your own website. Before you publish it on the
        internet, you want to make it look attractive, professional, or at least
        decent to look at.
      </p>
      <h2>Content</h2>
      <pre>The purpose of design is to enhance the presentation of the content it's
applied to. It might sound obvious, but content being the primary element
of a website, it should not be established as an afterthought.</pre>
      <p>
        The purpose of design is to enhance the presentation of the content it's
        applied to. It might sound obvious, but content being the primary element
        of a website, it should not be established as an afterthought. For more,
        see <a href="https://ruckless.xyz">this link</a>.
      </p>
    </main>
  </body>
</html>
