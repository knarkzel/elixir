<header>
  <div id="nav">
    <a href="/">Elixir</a>
    <nav>
      <% if user.is_some() { %>
        <a href="#"><%= user.as_ref().unwrap().email %></a>
      <% } else { %>
        <a href="/login">Login</a>
        / 
        <a href="/register">Register</a>
      <% } %>
    </nav>
  </div>
</header>
